diff --git a/linux/mm/huge_memory.c b/linux/mm/huge_memory.c
index 000000000000..000000000000 100644
--- a/linux/mm/huge_memory.c
+++ b/linux/mm/huge_memory.c
@@ -2063,6 +2063,15 @@ vm_fault_t do_huge_pmd_numa_page(struct vm_fault *vmf)
 		nid = NUMA_NO_NODE;
 	if (target_nid == NUMA_NO_NODE)
 		goto out_map;
+	/*
+	 * ICCD fault-only experiment:
+	 *
+	 * Keep NUMA hint-fault accounting and mapping rebuilds, but skip the
+	 * automatic misplaced-folio migration path.  Revert this patch after
+	 * collecting the fault-only control run.
+	 */
+	goto out_map;
+
 	if (migrate_misplaced_folio_prepare(folio, vma, target_nid)) {
 		flags |= TNF_MIGRATE_FAIL;
 		goto out_map;
diff --git a/linux/mm/memory.c b/linux/mm/memory.c
index 000000000000..000000000000 100644
--- a/linux/mm/memory.c
+++ b/linux/mm/memory.c
@@ -6076,6 +6076,15 @@ static vm_fault_t do_numa_page(struct vm_fault *vmf)
 		nid = NUMA_NO_NODE;
 	if (target_nid == NUMA_NO_NODE)
 		goto out_map;
+	/*
+	 * ICCD fault-only experiment:
+	 *
+	 * Keep NUMA hint-fault accounting and mapping rebuilds, but skip the
+	 * automatic misplaced-folio migration path.  Revert this patch after
+	 * collecting the fault-only control run.
+	 */
+	goto out_map;
+
 	if (migrate_misplaced_folio_prepare(folio, vma, target_nid)) {
 		flags |= TNF_MIGRATE_FAIL;
 		goto out_map;
