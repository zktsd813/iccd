# Checkpoint: physical 8G all workload ours-toggle w5

This checkpoint was captured while the VM experiment was still running.

Completed:

- `pr`: completed, return code 0.
- `bc`: completed, return code 0.
- `silo`: completed on resume, return code 0. The earlier interrupted run left a `rc=143` entry in `orchestrator.log`; the final `summary.csv` row reflects the resumed successful run.

In progress at checkpoint:

- `liblinear`: running when this checkpoint was copied from the guest.

Not started yet in this checkpoint:

- `FT`
- `LU`
- `SP`
- `gups`
- `graph500`
- `btree`
- `xsbench`

The experiment is continuing after this checkpoint.
